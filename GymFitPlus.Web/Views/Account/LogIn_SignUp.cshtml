@{
    ViewBag.Title = "LogIn/SignUp";
}

@section Styles {
    <link rel="stylesheet" href="~/css/Login_Register.css" asp-append-version="true" />
}

<div class="section">
    <div class="container">
        <div class="row full-height justify-content-center">
            <div class="col-12 text-center py-5">
                <div class="section pb-5 pt-5 pt-sm-2 text-center">
                    <h6 class="mb-0 pb-3"><span>Log In </span><span>Sign Up</span></h6>
                    <input class="checkbox" type="checkbox" id="reg-log" name="reg-log" />
                    <label for="reg-log"></label>
                    <div class="card-3d-wrap mx-auto">
                        <div class="card-3d-wrapper">
                            <div class="card-front">
                                <div class="center-wrap">
                                    @await Component.InvokeAsync("LoginComponent")
                                </div>
                            </div>
                            <div class="card-back">
                                <div class="center-wrap">
                                    @await Component.InvokeAsync("RegisterComponent")
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@if (ViewBag.Register != null && ViewBag.Register)
{
    <script>
        document.addEventListener('DOMContentLoaded', function () {

            var checkbox = document.getElementById('reg-log');
            checkbox.click();
        });
    </script>
}

@* @section Scripts {
    <partial name="_ValidationScriptsPartial" />
} *@